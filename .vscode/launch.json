{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Voxveil: Run App",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}",
      "windows": {
        "command": "npm run dev:windows"
      }
    }
  ]
}
